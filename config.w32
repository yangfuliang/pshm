// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("pshm", "for pshm support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("pshm", "enable pshm support", "no");

if (PHP_PSHM != "no") {
	EXTENSION("pshm", "pshm.c");
}

